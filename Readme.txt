1. Open Install Libraries.bat (Allow if a prompt shows)
2. Wait to install libraries
3. After installing, open SentryLabs.
4. Check the prompts, and then the browser will open.
(Optional)
- If the datasets were updated, go to Side Nav, and then press Update. You can also train the models by pressing Train on sidenav.